

32 32 32
15
chmod: cannot access './wait-fs': No such file or directory
mv: cannot stat './wait-fs': No such file or directory
ln: failed to create symbolic link '/mnt/flux/view/lib/python': File exists
ln: failed to create symbolic link '/mnt/flux/view/lib/python3': File exists
#!/bin/bash
set -euo pipefail
flux resource list
echo "The parent process ID is: $PPID"          
echo "The execution parent process ID is: $$"         
CGROUP_PATH_LINE=$(cat "/proc/$$/cgroup")
echo 
CGROUP_V2_PATH=${CGROUP_PATH_LINE:3}
ACTUAL_CGROUP_DIR="/sys/fs/cgroup${CGROUP_V2_PATH}"
TARGET_CGROUP_ID=$(stat -c '%i' $ACTUAL_CGROUP_DIR)
echo "The cgroup id is $TARGET_CGROUP_ID"
echo -n $TARGET_CGROUP_ID > /mnt/flux/cgroup-id.txt
sleep 5
flux exec -r all touch /mnt/flux/start_ebpf_collection
for i in {1..3}
do
  echo "FLUX-RUN START hpcg-iter-$i"
  flux run --setattr=user.study_id=hpcg-iter-$i -N1 -n 24 -o cpu-affinity=per-task -o gpu-affinity=off     /opt/hpcg/xhpcg
  
   echo "FLUX-RUN END hpcg-iter-$i"
done

  echo "FLUX-RESULTS START"
  cat hpcg*.txt
  echo "HPCG-BENCHMARKS"
  cat HPCG-Benchmark*.txt
  echo "FLUX-RESULTS END"

output=./results/${app}
(apt-get update > /dev/null 2>&1 && apt-get install -y jq > /dev/null 2>&1) || (yum update -y > /dev/null 2>&1 && yum install -y jq > /dev/null 2>&1)
mkdir -p $output
for jobid in $(flux jobs -a --json | jq -r .jobs[].id); do
    echo
    study_id=$(flux job info $jobid jobspec | jq -r ".attributes.user.study_id")
    echo "FLUX-JOB START ${jobid} ${study_id}"
    echo "FLUX-JOB-JOBSPEC START"
    flux job info $jobid jobspec
    echo "FLUX-JOB-JOBSPEC END" 
    
    echo "FLUX-JOB-RESOURCES START"
    flux job info ${jobid} R
    echo "FLUX-JOB-RESOURCES END"
    echo "FLUX-JOB-EVENTLOG START" 
    flux job info $jobid guest.exec.eventlog
    echo "FLUX-JOB-EVENTLOG END" 
    echo "FLUX-JOB END ${jobid} ${study_id}"
done
echo "FLUX JOB STATS"
flux job stats 

     STATE NNODES   NCORES    NGPUS NODELIST
      free      1       24        0 hpcg-0
 allocated      0        0        0 
      down      0        0        0 
The parent process ID is: 119
The execution parent process ID is: 293

The cgroup id is 34413
FLUX-RUN START hpcg-iter-1
56.338s: flux-shell[0]: FATAL: doom: rank 0 exited and exit-timeout=30s has expired
56.340s: job.exception type=exec severity=0 rank 0 exited and exit-timeout=30s has expired
flux-job: task(s) exited with exit code 132
